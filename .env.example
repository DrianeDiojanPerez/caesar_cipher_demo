VITE_API_BASE_URL=https://caesar-cipher-demo-backend.fly.dev

# Leaderboard refresh interval, in milliseconds. Lower = livelier UI,
# more requests. Clamped to a 500ms minimum at runtime.
VITE_LEADERBOARD_POLL_MS=3000
